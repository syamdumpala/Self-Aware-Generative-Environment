[project]
name = "sage-context-layer"
version = "0.1.0"
description = "Local hardened delivery surface for the SAGE AI PC concept"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
